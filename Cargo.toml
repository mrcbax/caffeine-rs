[package]
name = "caffeine"
version = "0.1.0"
authors = ["Chad Baxter <cbaxter@mail.umw.edu>"]

[dependencies]
chrono = "0.3.0"
toml = "0.3"
serde = "0.9.8"
serde_derive = "0.9.8"

[target.'cfg(unix)'.dependencies]
uinput = "0.1.2"
[target.'cfg(windows)'.dependencies]
keystroke = "0.0.3"
